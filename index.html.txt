<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>YourGoatViper — Official</title>
  <meta name="description" content="Official website of YourGoatViper (Arham Arain) — PUBG content creator, 34x tournament winner, TDM player, Pakistan #3. International tournament: live streams with commentary." />
  <style>
    /* ---------- Reset ---------- */
    *{box-sizing:border-box;margin:0;padding:0}
    html,body{height:100%;font-family:Inter,system-ui,-apple-system,"Segoe UI",Roboto,Arial; background:#000;color:#fff}
    a{color:inherit;text-decoration:none}
    button{font:inherit}

    /* ---------- Background poster (exact visual) ---------- */
    .bg{
      position:fixed;inset:0;background-image:url('IMG_1703.jpeg');
      background-size:cover;background-position:center center;filter:brightness(.45);
      z-index:-3;
    }
    .overlay{
      position:fixed;inset:0;background:linear-gradient(90deg, rgba(0,0,0,.6) 0%, rgba(0,0,0,.55) 60%, rgba(0,0,0,.3) 100%);
      z-index:-2;
    }

    /* ---------- Header / Nav ---------- */
    header{
      position:fixed;left:0;right:0;top:18px;display:flex;align-items:center;justify-content:space-between;padding:14px 28px;z-index:60;
    }
    .brand{color:#ff6a2b;font-weight:800;letter-spacing:1px}
    nav{display:flex;gap:20px;align-items:center}
    nav a{color:#fff;opacity:.95;padding:8px 10px;border-radius:6px;transition:background .18s,transform .12s}
    nav a:hover{background:rgba(255,255,255,.04);transform:translateY(-3px)}
    nav a.active{box-shadow: inset 0 -2px 0 0 #ff6a2b}

    /* ---------- Page layout ---------- */
    main{min-height:100vh;display:flex;flex-direction:column;justify-content:center;padding:8vh 6vw 10vh 6vw}
    .hero{display:flex;gap:2.5rem;align-items:flex-start}
    .left{max-width:780px;animation:fadeInUp .9s both}
    .subtitle{font-size:12px;letter-spacing:6px;color:rgba(255,255,255,.8);text-transform:uppercase;margin-bottom:12px}
    .title{font-size:78px;line-height:.95;font-weight:900;color:#e35a23;text-shadow:0 6px 18px rgba(0,0,0,.6);margin-bottom:18px}
    .cta-row{display:flex;gap:14px;align-items:center;margin-bottom:18px}
    .btn{
      background:linear-gradient(180deg,#bf4a1d,#e36b2b);
      color:#fff;padding:12px 24px;border-radius:8px;border:none;cursor:pointer;font-weight:800;
      box-shadow:0 8px 30px rgba(227,107,43,.12);transition:transform .2s,box-shadow .2s;
    }
    .btn:hover{transform:translateY(-5px);box-shadow:0 20px 50px rgba(227,107,43,.18)}
    .btn.ghost{background:transparent;border:1px solid rgba(255,255,255,.08);box-shadow:none}

    /* ---------- Intro card (kept visible) ---------- */
    .intro-card{
      margin-top:6px;background:rgba(255,255,255,.03);padding:18px;border-radius:12px;display:flex;gap:16px;align-items:flex-start;
      border:1px solid rgba(255,255,255,.04);backdrop-filter:blur(4px);
      transform-origin:left center;animation:float 5s ease-in-out infinite;
    }
    .intro-left{flex:1}
    .intro-left h3{font-size:18px;margin-bottom:6px}
    .intro-left p{color:rgba(255,255,255,.85);line-height:1.4;font-size:14px}
    .intro-right{display:flex;flex-direction:column;gap:10px;align-items:center}

    /* ---------- Tournament block (separate, under intro) ---------- */
    .tournament{
      margin-top:20px;display:flex;gap:18px;flex-wrap:wrap;align-items:center;
      animation:fadeIn .8s both;
    }
    .tourney-desc{flex-basis:100%;background:linear-gradient(180deg, rgba(255,255,255,.02), rgba(255,255,255,.01));padding:14px;border-radius:10px;border:1px solid rgba(255,255,255,.04)}
    .tourney-desc strong{color:#fff}

    /* Entry & Prize boxes */
    .boxes{display:flex;gap:12px;flex-wrap:wrap;margin-top:10px}
    .box{
      min-width:140px;padding:14px;border-radius:10px;background:linear-gradient(180deg, rgba(255,255,255,.02), rgba(0,0,0,.15));
      border:1px solid rgba(255,255,255,.04);text-align:center;box-shadow:0 10px 30px rgba(0,0,0,.45);transition:transform .18s,box-shadow .18s;
    }
    .box:hover{transform:translateY(-8px);box-shadow:0 26px 60px rgba(0,0,0,.55)}
    .box .label{font-size:13px;color:rgba(255,255,255,.8);margin-bottom:8px}
    .box .value{font-weight:900;font-size:20px}

    /* ---------- Right area (kept empty so poster stays intact) ---------- */
    .right{flex:1;min-height:320px;display:flex;align-items:center;justify-content:center}

    /* ---------- Modal for Continue ---------- */
    .modal{
      position:fixed;inset:0;display:flex;align-items:center;justify-content:center;padding:28px;
      visibility:hidden;opacity:0;transition:opacity .22s ease,visibility .22s;z-index:90;
    }
    .modal.visible{visibility:visible;opacity:1}
    .backdrop{position:absolute;inset:0;background:rgba(0,0,0,.65);backdrop-filter:blur(2px)}
    .modal-card{position:relative;z-index:2;max-width:920px;width:100%;display:grid;grid-template-columns:1fr 320px;gap:18px;padding:22px;border-radius:12px;background:linear-gradient(180deg,#0b0b0b,#0f0f0f);border:1px solid rgba(255,255,255,.04);box-shadow:0 28px 80px rgba(0,0,0,.6)}
    .social-grid{display:flex;flex-wrap:wrap;gap:12px}
    .social{display:flex;align-items:center;gap:12px;padding:10px 12px;border-radius:10px;background:linear-gradient(180deg,rgba(255,255,255,.01),rgba(255,255,255,.005));min-width:170px;border:1px solid rgba(255,255,255,.03);cursor:pointer;transition:transform .15s}
    .social:hover{transform:translateY(-6px)}
    .social .icon{width:28px;height:28px;flex-shrink:0;border-radius:6px;display:inline-grid;place-items:center;background:rgba(255,255,255,.04)}
    .qr-wrap{display:flex;flex-direction:column;align-items:center;gap:8px}

    /* ---------- About & Contact panels (slide from right) ---------- */
    .panel{
      position:fixed;top:0;right:-100%;height:100vh;width:420px;background:linear-gradient(180deg,#071017,#0f1a1f);
      z-index:85;padding:28px;box-shadow:-18px 0 60px rgba(0,0,0,.7);transition:right .36s cubic-bezier(.2,.9,.18,1)
    }
    .panel.open{right:0}
    .panel h3{margin-bottom:10px}
    .panel p{color:rgba(255,255,255,.9);line-height:1.45;margin-bottom:14px}
    .links{display:flex;flex-direction:column;gap:8px}

    /* ---------- Footer hint ---------- */
    .hint{position:fixed;left:18px;bottom:18px;color:rgba(255,255,255,.8);font-size:13px;z-index:60}

    /* ---------- Animations ---------- */
    @keyframes fadeInUp{from{opacity:0;transform:translateY(14px)}to{opacity:1;transform:translateY(0)}}
    @keyframes float{0%{transform:translateY(0)}50%{transform:translateY(-6px)}100%{transform:translateY(0)}}
    @keyframes fadeIn{from{opacity:0}to{opacity:1}}

    /* ---------- Responsive ---------- */
    @media (max-width:980px){
      .title{font-size:48px}
      .modal-card{grid-template-columns:1fr}
      .panel{width:100%}
      .intro-card{flex-direction:column;align-items:flex-start}
    }
    @media (max-width:520px){
      .title{font-size:34px}
      header{padding:10px 14px}
      nav{gap:8px}
      .modal-card{padding:14px}
    }
  </style>
</head>
<body>

  <!-- Background images (keep unchanged poster look) -->
  <div class="bg" aria-hidden="true"></div>
  <div class="overlay" aria-hidden="true"></div>

  <!-- Header -->
  <header>
    <div class="brand">ARHAM ARAIN</div>
    <nav>
      <a href="#home" id="nav-home" class="active">Home</a>
      <a href="#about" id="nav-about">About</a>
      <a href="#contact" id="nav-contact">Contact</a>
    </nav>
  </header>

  <!-- Main content -->
  <main id="home" role="main">
    <section class="hero" aria-labelledby="heroTitle">
      <div class="left">
        <div class="subtitle">YOU GOAT VIPER OFFICIAL WEBSITE</div>
        <h1 class="title" id="heroTitle">YOURGOAT<br>VIPER</h1>

        <div class="cta-row">
          <button class="btn" id="continueBtn">Continue</button>
          <button class="btn ghost" id="scrollTournament">Tournament Info</button>
        </div>

        <!-- Introduction (separate from tournament) -->
        <div class="intro-card" aria-labelledby="introTitle">
          <div class="intro-left">
            <h3 id="introTitle">YourGoatViper — Introduction</h3>
            <p>
              <strong>YourGoatViper</strong> (Arham Arain) — Pubg content creator 🔥, <strong>34 tournament winner</strong> 🥇, TDM player, Pakistan #3 player. Account seller, GFX designer, and editor.
            </p>
            <p style="margin-top:8px;color:rgba(255,255,255,.8);font-size:13px">
              For tournament registration and media inquiries contact via WhatsApp or the social links in the Continue modal.
            </p>
          </div>

          <div class="intro-right" aria-hidden="true">
            <!-- Separate boxes (entry/prize) kept visually here as quick info (also repeated in tournament section) -->
            <div class="box" style="min-width:120px;padding:10px">
              <div class="label">Entry Fee</div>
              <div class="value">200</div>
            </div>
            <div class="box" style="min-width:160px;padding:10px">
              <div class="label">Prize Pool</div>
              <div class="value">5K + Int'l entry free</div>
            </div>
          </div>
        </div>

        <!-- Tournament section (separate & below intro) -->
        <div class="tournament" id="tournament">
          <div class="tourney-desc">
            <strong>International Tournament</strong>
            <p style="margin-top:8px;color:rgba(255,255,255,.9)">
              YourGoatViper is organising an international PUBG tournament. Many international players will participate. All matches will be <strong>live-streamed</strong> with commentary. Register via WhatsApp or the social links provided.
            </p>
          </div>

          <div class="boxes">
            <div class="box">
              <div class="label">Entry Fee</div>
              <div class="value">200</div>
            </div>
            <div class="box">
              <div class="label">Prize Pool</div>
              <div class="value">5,000 PKR</div>
            </div>
            <div class="box">
              <div class="label">International Entry</div>
              <div class="value">Free</div>
            </div>
          </div>
        </div>

      </div>

      <!-- Right side intentionally empty so poster visuals (basketball & flames) remain unchanged -->
      <div class="right" aria-hidden="true"></div>
    </section>
  </main>

  <div class="hint">Use the top menu to view About & Contact — or press Continue</div>

  <!-- Continue modal (socials + QR) -->
  <div class="modal" id="modal" role="dialog" aria-hidden="true" aria-labelledby="modalTitle">
    <div class="backdrop" id="modalBackdrop"></div>
    <div class="modal-card" role="document">
      <div>
        <h2 id="modalTitle">Connect with YourGoatViper</h2>
        <p style="color:rgba(255,255,255,.78);margin-bottom:12px">Tap any link to open (WhatsApp has a prefilled message).</p>

        <div class="social-grid">
          <a class="social" id="link-wa" href="https://wa.me/923348666640?text=Hello%20YourGoatViper%F0%9F%94%A5%2C%20I%20want%20to%20join%20the%20tournament!" target="_blank" rel="noopener noreferrer" title="WhatsApp">
            <div class="icon">WA</div><div>WhatsApp</div>
          </a>

          <a class="social" href="https://www.instagram.com/arham_arain333?igsh=NGhobDk1c29rd3oy&utm_source=qr" target="_blank" rel="noopener noreferrer" title="Instagram">
            <div class="icon">IG</div><div>Instagram</div>
          </a>

          <a class="social" href="https://snapchat.com/t/pJwMTBOf" target="_blank" rel="noopener noreferrer" title="Snapchat">
            <div class="icon">SC</div><div>Snapchat</div>
          </a>

          <a class="social" href="https://www.youtube.com/@YourGoatViper" target="_blank" rel="noopener noreferrer" title="YouTube">
            <div class="icon">YT</div><div>YouTube</div>
          </a>

          <a class="social" href="https://www.tiktok.com/@yourgoatviper?_t=ZS-90S6zQCiZwS&_r=1" target="_blank" rel="noopener noreferrer" title="TikTok">
            <div class="icon">TT</div><div>TikTok</div>
          </a>
        </div>

        <div style="margin-top:12px;color:rgba(255,255,255,.75)"><strong>WhatsApp:</strong> +92 334 8666640</div>
      </div>

      <div class="qr-wrap">
        <div style="background:#fff;padding:12px;border-radius:10px">
          <img src="IMG_1698.jpeg" alt="WhatsApp QR" style="width:220px;height:220px;display:block">
        </div>
        <div style="font-size:13px;color:rgba(255,255,255,.8)">Scan or tap WhatsApp to message directly</div>
        <div style="display:flex;gap:8px;margin-top:12px">
          <button class="btn" onclick="window.open('https://wa.me/923348666640?text=Hello%20YourGoatViper%F0%9F%94%A5%2C%20I%20want%20to%20join%20the%20tournament!','_blank')">WhatsApp</button>
          <button class="btn" style="background:linear-gradient(180deg,#333,#222)" id="closeModalBtn">Close</button>
        </div>
      </div>
    </div>
  </div>

  <!-- About & Contact slide panels -->
  <aside class="panel" id="aboutPanel" aria-hidden="true">
    <h3>About</h3>
    <p>
      Owner of this website is <strong>YourGoatViper</strong> (Arham Arain). This site is for tournament updates, content announcements and direct contact.
    </p>
    <div class="links">
      <a href="https://wa.me/923348666640" target="_blank" rel="noopener noreferrer">WhatsApp — +92 334 8666640</a>
      <a href="https://www.instagram.com/arham_arain333?igsh=NGhobDk1c29rd3oy&utm_source=qr" target="_blank" rel="noopener noreferrer">Instagram — arham_arain333</a>
      <a href="https://snapchat.com/t/pJwMTBOf" target="_blank" rel="noopener noreferrer">Snapchat — Aarain21553</a>
      <a href="https://www.tiktok.com/@yourgoatviper?_t=ZS-90S6zQCiZwS&_r=1" target="_blank" rel="noopener noreferrer">TikTok — yourgoatviper</a>
      <a href="https://www.youtube.com/@YourGoatViper" target="_blank" rel="noopener noreferrer">YouTube — YourGoatViper</a>
    </div>
  </aside>

  <aside class="panel" id="contactPanel" aria-hidden="true">
    <h3>Contact</h3>
    <p>Direct contact details & quick links.</p>
    <div class="links">
      <a href="https://wa.me/923348666640?text=Hello%20YourGoatViper%F0%9F%94%A5%2C%20I%20want%20to%20join%20the%20tournament!" target="_blank" rel="noopener noreferrer">WhatsApp: +92 334 8666640</a>
      <a href="https://www.instagram.com/arham_arain333?igsh=NGhobDk1c29rd3oy&utm_source=qr" target="_blank" rel="noopener noreferrer">Instagram: arham_arain333</a>
      <a href="https://snapchat.com/t/pJwMTBOf" target="_blank" rel="noopener noreferrer">Snapchat: Aarain21553</a>
    </div>
  </aside>

<script>
  // ---------- Basic DOM references ----------
  const modal = document.getElementById('modal');
  const modalBackdrop = document.getElementById('modalBackdrop');
  const continueBtn = document.getElementById('continueBtn');
  const closeModalBtn = document.getElementById('closeModalBtn');
  const navHome = document.getElementById('nav-home');
  const navAbout = document.getElementById('nav-about');
  const navContact = document.getElementById('nav-contact');
  const aboutPanel = document.getElementById('aboutPanel');
  const contactPanel = document.getElementById('contactPanel');
  const scrollTournamentBtn = document.getElementById('scrollTournament');
  const tournamentSection = document.getElementById('tournament');

  // ---------- Modal open/close ----------
  continueBtn.addEventListener('click', () => {
    modal.classList.add('visible');
    modal.setAttribute('aria-hidden','false');
  });
  modalBackdrop.addEventListener('click', closeModal);
  closeModalBtn.addEventListener('click', closeModal);
  function closeModal(){
    modal.classList.remove('visible');
    modal.setAttribute('aria-hidden','true');
  }
  document.addEventListener('keydown', (e) => { if (e.key === 'Escape') closeModal(); });

  // ---------- Navigation (work & animate) ----------
  function setActiveLink(activeEl){
    [navHome, navAbout, navContact].forEach(a => a.classList.remove('active'));
    activeEl.classList.add('active');
  }

  navHome.addEventListener('click', (e) => {
    e.preventDefault();
    // close panels and modal, scroll to top (home)
    aboutPanel.classList.remove('open'); contactPanel.classList.remove('open'); closeModal();
    setActiveLink(navHome);
    window.scrollTo({top:0,behavior:'smooth'});
  });

  navAbout.addEventListener('click', (e) => {
    e.preventDefault();
    aboutPanel.classList.add('open'); contactPanel.classList.remove('open'); closeModal();
    setActiveLink(navAbout);
  });

  navContact.addEventListener('click', (e) => {
    e.preventDefault();
    contactPanel.classList.add('open'); aboutPanel.classList.remove('open'); closeModal();
    setActiveLink(navContact);
  });

  // Close panels with Escape or click outside (keyboard)
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') { aboutPanel.classList.remove('open'); contactPanel.classList.remove('open'); }
  });

  // ---------- Tournament quick scroll ----------
  scrollTournamentBtn.addEventListener('click', ()=> {
    tournamentSection.scrollIntoView({behavior:'smooth',block:'center'});
  });

  // make QR image clickable to open in new tab
  document.querySelectorAll('.qr-wrap img').forEach(img => {
    img.style.cursor = 'pointer';
    img.addEventListener('click', ()=> window.open(img.src, '_blank'));
  });

  // Accessibility: close panels when clicking outside them
  document.addEventListener('click', (e) => {
    const panels = [aboutPanel, contactPanel];
    // if click inside panel, ignore
    if (panels.some(p => p.contains(e.target))) return;
    // if click nav links, ignore because nav handlers manage it
    if (e.target.closest('nav')) return;
    // else close panels
    panels.forEach(p => p.classList.remove('open'));
    setActiveLink(navHome);
  });
</script>

</body>
</html>
